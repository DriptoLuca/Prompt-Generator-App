name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.22.0'
          
     - name: Accetta licenze Android
        run: |
          # Installazione forzata dei componenti necessari
          yes | sdkmanager --licenses > /dev/null || true
          
      - name: Build APK
        run: |
          pip install flet
          # Proviamo a compilare accettando le licenze durante il build
          flet build apk --yes

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Generatore-Prompt-APK
          path: build/apk/**/*.apk
